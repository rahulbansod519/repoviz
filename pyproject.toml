[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "repoviz"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["click>=8.1", "openai>=1.0"]

[project.scripts]
repoviz = "repoviz:main"

[tool.setuptools]
py-modules = ["repoviz"]
